# 🛠 Infrastructure & QA Automation Utilities

Набор Python-скриптов для автоматизации рутинных задач системного администрирования и QA. 
Репозиторий содержит инструменты для мониторинга состояния системы, проверки целостности данных и анализа логов.

## 🎯 Описание инструментов

В данный момент реализованы базовые утилиты, необходимые для поддержки тестовых стендов (Linux) и валидации данных в СХД.

### 1. `data_integrity.py` (Data Integrity Check)
Скрипт для валидации целостности файлов.
- **Принцип работы:** Вычисляет SHA-256 хеш файла до и после операций (имитация передачи данных или миграции).
- **Зачем это нужно:** Критически важно для тестирования СХД и систем хранения. Позволяет обнаружить "тихое" повреждение данных (bit rot), которое невозможно заметить при простой проверке размера файла.
- **Технологии:** `hashlib`, поблочное чтение файлов (для работы с большими объемами данных без переполнения RAM).

### 2. `disk_check.py` (System Health Monitor)
Утилита для мониторинга свободного места на диске.
- **Принцип работы:** Использует системный вызов `df -h` через `subprocess`, парсит вывод и проверяет использование диска относительно заданного порога (Threshold).
- **Зачем это нужно:** Предотвращает падение тестов из-за нехватки места на стенде. Может использоваться как часть CI/CD пайплайна перед запуском тяжелых тестов.
- **Технологии:** `subprocess`, string parsing.

### 3. `log_parser.py` (Log Analysis Tool)
Парсер системных логов для поиска критических ошибок.
- **Принцип работы:** Сканирует лог-файлы (например, `/var/log/syslog` или логи приложения) на наличие ключевых слов (`ERROR`, `FAIL`, `DEGRADED`, `panic`).
- **Зачем это нужно:** Автоматизирует анализ результатов нагрузочного тестирования. Помогает быстро найти причину сбоя без ручного просмотра тысяч строк логов.
- **Технологии:** File I/O, Context Managers, Error Handling.

---

## 🚀 Как запустить

Для работы требуется Python 3.6+. Дополнительные библиотеки устанавливать не нужно (используется Standard Library).

### Проверка целостности данных:
```bash
python3 data_integrity.py

---

## 🛠 Стек технологий

Язык: Python 3
OS: Linux (Ubuntu/CentOS), macOS
Модули: subprocess, hashlib, os, sys

## 🔜 Планы по развитию
[ ] Добавить отправку алертов в Telegram/Email при обнаружении ошибок.
[ ] Реализовать подключение по SSH (paramiko) для проверки удаленных узлов.
[ ] Добавить многопоточность для проверки больших массивов файлов.